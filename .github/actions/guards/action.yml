name: Consumer Guards
description: Guards (tests, lint, e2e) specific to this repo

runs:
  using: composite
  steps:
    - name: Run CI
      shell: bash
      run: |
        echo "🧪 consumer-test: running CI hook"
        echo "  → In react-email this would be: pnpm test + Playwright"
        echo "  → In resend-python this would be: pytest"
        echo "  ✓ All checks passed"
