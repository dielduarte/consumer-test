name: Send to Production

on:
  workflow_dispatch:

jobs:
  send-to-production:
    uses: dielduarte/shared-workflows/.github/workflows/js-send-to-production.yml@main
    permissions:
      contents: write
      actions: write
    secrets: inherit
