name: Canary Release

on:
  push:
    branches:
      - canary

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    uses: dielduarte/shared-workflows/.github/workflows/js/canary-release.yml@main
    permissions:
      id-token: write
      contents: read
    secrets: inherit
